language: node_js
node_js:
  - 14
sudo: required
services:
  - docker

before_install: 
  - docker build -t karthik/docker -f Dockerfile.dev .

# script: 
#   - docker run karthik/docker -- --coverage